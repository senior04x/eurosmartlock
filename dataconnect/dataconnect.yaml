specVersion: "v1beta"
serviceId: "eurosmartlock"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "eurosmartlock-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
